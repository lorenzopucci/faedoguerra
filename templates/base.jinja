<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="/static/favicon.ico">

  <link rel="stylesheet" href="/static/styles/theme.css">
  <link rel="stylesheet" href="/static/styles/header.css">
  <script src="/static/scripts/header.js"></script>
  {% block head %}{% endblock %}
</head>
<body>
  <header>
    <div id="header-inner">
      <span>
        <a href="/">
          <span id="main-title">Guerra del Faedo</span>
        </a>
      </span>

      <button id="collapse-btn" onclick="toggle_collapse_navbar()">&#9776</button>
      <div id="header-links">
        <span onclick="redirect_to_dashboard();">
          <span>Dashboard</span>
          <img src="/static/icons/globe.svg" height="22">
        </span>
        <span onclick="redirect_to_about();">
          <span>Informazioni</span>
          <img src="/static/icons/about.svg" height="22">
        </span>

        {% if user.is_authenticated %}
          <span onclick="redirect_to_user();">
            <span>{{user.get_username()}}</span>
            <img src="/static/icons/user.svg" height="22">
          </span>
        {% else %}
          <span onclick="redirect_to_login();">
            <span>Accedi</span>
            <img src="/static/icons/login.svg" height="22">
          </span>
        {% endif %}
      </div>
    </div>
  </header>

  <div id="page-wrapper">
    {% block content %}{% endblock %}
  </div>
</body>
</html>
